services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=dev
      - JWT_SECRET=dev-insecure-secret-change-me
    restart: unless-stopped
